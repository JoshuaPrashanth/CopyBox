#include <iostream>
using namespace std;

int main() {

  string x;

  cin >> x;  // input is provided by the grader

  if (x == "red" || x == "blue" || x == "yellow") {
    cout << x << " is a primary color";
  } else {
    cout << x << " is not a primary color";
  }

  return 0;
}
